# Prisma CLI reads this from the workspace root (shared prisma/ schema, per
# 6-development/2-folder-structure.md §4). Copy to .env and fill in real local values.
# Full env-var documentation (backend/frontend split, all required/optional keys):
# 6-development/1-development-environment.md §9 — expanded in T-006.

DATABASE_URL="postgresql://postgres:postgres@localhost:5432/lbm_erp_dev?schema=public"
