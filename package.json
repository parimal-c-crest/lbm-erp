{
  "name": "lbm-erp-rewrite",
  "version": "0.1.0",
  "private": true,
  "description": "LBM ERP Rewrite — pnpm workspace monorepo (backend: NestJS, frontend: Next.js)",
  "engines": {
    "node": ">=24",
    "pnpm": ">=10"
  },
  "packageManager": "pnpm@11.15.1",
  "scripts": {
    "postinstall": "prisma generate",
    "dev": "pnpm --parallel --filter backend --filter frontend run dev",
    "build": "pnpm --filter backend run build && pnpm --filter frontend run build",
    "lint": "pnpm --filter backend run lint && pnpm --filter frontend run lint",
    "typecheck": "pnpm --filter backend run typecheck && pnpm --filter frontend run typecheck",
    "format": "prettier --write \"backend/{src,test}/**/*.ts\" \"frontend/src/**/*.{ts,tsx}\"",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.13.0",
    "@playwright/test": "^1.62.1",
    "eslint-plugin-import": "^2.31.0",
    "prettier": "^3.4.2",
    "prettier-plugin-tailwindcss": "^0.8.1",
    "prisma": "^7.9.1"
  }
}
