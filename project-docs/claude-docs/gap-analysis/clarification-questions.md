# Clarification Questions

## Blocking — resolved with developer, locked in `decisions-log.md`

| # | Question | Answer | ADR |
|---|---|---|---|
| Q-01 | Is legacy-system security remediation in scope for this project? | Rewrite only — legacy fixes out of scope. | ADR-001 |
| Q-02 | What role catalog should the new system use, given the legacy system has none? | Inferred 6-role starter list (Counter/Sales, Warehouse/Fulfillment, Accounting/Management, Purchasing, Admin, B2B Customer). | ADR-002 |
| Q-03 | Which long-term module-count figure is authoritative — 93 or 111? | Neither — legacy figures are sometimes simply wrong; only MVP-18 is treated as firm. | ADR-003 |

No further Blocking items — cleared to proceed to `2-document-plan/1-documentation-plan.md`.

## Non-blocking — carried forward as documented assumptions, revisit when the relevant document generates

| # | Question | Relevant document | Default assumption if not answered before generation |
|---|---|---|---|
| Q-04 | What are this project's measurable business success criteria (beyond "close-by-construction security")? | `1-project/1-project-overview.md` §14 | Flag `[NEEDS INPUT]` at generation — do not invent numbers. |
| Q-05 | What's the legacy-to-new data migration/cutover strategy? | `2-database/3-migration-strategy.md` | Flag `[NEEDS INPUT]` — not resolvable from SoT, needs infra-owner input (also blocked on hosting decision, already deferred). |
| Q-06 | What are the numeric NFR targets (response time, uptime %, throughput)? | `7-cross-cutting/1-non-functional-requirements.md` §2-3 | Flag `[NEEDS INPUT]` — SoT explicitly states these as unassessed. |
| Q-07 | Is MFA required? What are the API rate-limit numbers? | `3-api/2-authentication.md` §11, §15 | Flag `[NEEDS INPUT]` for both. |
| Q-08 | CI/CD platform? Git branching model? WCAG accessibility target level? | `6-development/9-ci-cd.md`, `4-git-workflow.md`, `4-ui/7-accessibility.md` | Flag `[NEEDS INPUT]` for each; propose WCAG 2.1 AA as the industry-default recommendation if developer wants a quick default rather than a fresh decision. |
| Q-09 | What's the sprint capacity/pacing (no budget/timeline/sponsor in SoT)? | `7-sprint-planning/1-sprint-planning.md` | Ask developer directly when that phase runs — not a documentation gap, a planning input. |
| Q-10 | Pricebooklevel200 vs. 300's account-assignment column (`cf_984` vs `cf_986`) — same field or two? | ~~Pricebooklevel200/300~~ `pricing` `5-modules/.../4-schema.md` | **Superseded by ADR-029** — the unified `pricing` module designs its own schema, doesn't need to reconcile legacy column names 1:1. |
| Q-11 | Cross-tier pricing precedence (200 vs 300 vs 800) when multiple tiers could match an account? | ~~Pricebooklevel200/300/800~~ `pricing` `5-modules/.../3-business-rules.md` | **Superseded by ADR-029** — one unified engine gets one fresh precedence model designed at its own JIT cycle, not a reconciliation of three legacy behaviors. |
| Q-12 | Pricebooklevel300 coupon subsystem — build out for the new system, or retire? | `pricing` `5-modules/` JIT cycle | **Resolved by ADR-029** — build it in, wired to actually affect price (closing the legacy "dead-end"). |
| Q-13 | SalesHistory/PurchaseHistory canonical `total_activity` formula — confirm the recommended `|transfer_out_qty|`-wrapped variant? | SalesHistory/PurchaseHistory `5-modules/` JIT cycle | Ask developer (SME sign-off explicitly required per source) at that module's JIT cycle — do not silently adopt the 2-of-3-writers majority as fact. |
| Q-14 | Vendor "Title" field vs. "Sales Rep Name" field — same person represented two ways, or two distinct roles? | Vendors `5-modules/vendors/4-schema.md` | Ask LBM staff who actually enter/use these two fields — not resolvable from code, no relationship confirmed anywhere in legacy. |
| Q-15 | Saberis integration's two fields — one column named for "UOM" carries a "NS Code Template" label, and vice versa. Genuinely swapped labels, or a rename that never got fixed? | Vendors `5-modules/vendors/4-schema.md`, Saberis integration design | Ask whoever owns/configured the Saberis integration at LBM. |
| Q-16 | What does "ERF/MUF" mean (a pricing-formula flag on Vendor Line Code)? | Vendors `5-modules/vendors/3-business-rules.md` | Ask LBM staff — abbreviation never expanded anywhere in legacy source or docs. |
| Q-17 | What does "DCS" mean (4 related pricing fields: Copy To/Take/Formula/Value on Vendor Line Code)? Suspected door-configuration-system pricing integration, not confirmed. | Vendors `5-modules/vendors/3-business-rules.md` | Ask LBM staff — confirm before deciding whether/how to carry these fields forward. |
| Q-18 | "Default Door Code" on Vendor Line Code is stored as free text but defaults to a yes/no-shaped value ("No") — intentional, or should it be a real boolean? | Vendors `5-modules/vendors/4-schema.md` | Ask LBM staff what values this field actually holds in practice. |
| Q-19 | Are the X12 EDI fields (enablement, invoice option, vendor ID) on Vendors genuinely unused, safe to drop? | Vendors `5-modules/vendors/4-schema.md` | No code consumer found, but not exhaustively proven dead — confirm with LBM staff before dropping from the new schema. |
| Q-20 | What is "Level of Map" (a field on Vendor Line Code) for? | Vendors `5-modules/vendors/4-schema.md` | Ask LBM staff — no code usage or documentation found anywhere. |
| Q-21 | Why does a single "Product Number" field sit on Vendor Line Code, which is otherwise a line-level (not product-level) record? | Vendors `5-modules/vendors/4-schema.md` | Ask LBM staff — meaning of a per-row product number on a line-level record not confirmed. |
| Q-22 | Roughly 10 additional Vendor header/custom-field columns have no CRM label and no confirmed code usage anywhere — what are they for? | Vendors `5-modules/vendors/4-schema.md` | Needs a direct check against the live production database with LBM staff — can't be resolved from code alone. Full list: `sot-docs/raw/2-module-specs/Vendors/entities-and-fields.md` "Known Gaps"/orphan-column section. |
| Q-23 | Is SalesHistory's UOM field ever actually populated in production, for any tenant? | SalesHistory `5-modules/sales-history/4-schema.md` | Empty on every dev-snapshot row and no write site found anywhere — ask LBM staff if any live data ever fills this in before deciding whether to keep the field. |
| Q-24 | What was SalesHistory's "Price Tracker ID" field ever meant to reference? | SalesHistory `5-modules/sales-history/4-schema.md` | No label, no write site, zero live rows populated anywhere — ask LBM staff before dropping it; likely a true orphan. |
| Q-25 | Is SalesHistory's near-zero match rate between its stored product number and the live Products table a real production data-quality problem, or just an artifact of the dev snapshot checked? | SalesHistory `5-modules/sales-history/4-schema.md` | Ask LBM staff to check against real production data before assuming either explanation. |
| Q-26 | Does SalesHistory's lost-sale counter genuinely stay at zero in production, or is that a dev-data artifact? | SalesHistory `5-modules/sales-history/3-business-rules.md` | The write mechanism (a scheduled job) does exist and run — ask LBM staff whether it's actually producing real values in production. |
| Q-27 | PurchaseLineItem has two candidate "grouping" tables — which one (if either) was actually intended, and what's the group concept for? | PurchaseLineItem `5-modules/purchase-line-item/4-schema.md` | Neither table has real live data (1 row vs. 0) and no write-site code was found for either — ask LBM staff before deciding which (if any) to carry forward. |
| Q-28 | What is PurchaseHistory's "Group Name" field (on its own Group Relation entity) actually used for? | PurchaseHistory `5-modules/purchase-history/4-schema.md` | No write-site code found anywhere in legacy — ask LBM staff before deciding whether to carry it forward. |
| Q-29 | Settings has ~50 unclear/unconfirmed items in one batch: unexpanded abbreviations (OKB, COSH, GRT, "silo" tax component, ZDP/DCI, INI codes), unclear field relationships (dual logo storage, dual mail-account credential pairs, Company Profile vs. Organization Details table overlap, two parallel document-folder trees), several integration save-target tables never confirmed (FedEx/UPS/USPS, ExportingFTP, EliteExtra, Google Calendar OAuth storage, Traverse), and a handful of "is this feature/table actually used anywhere" questions (module-owner enforcement, announcement-table design intent, a couple of credential-mapping tables). | Settings `5-modules/settings/4-schema.md` and `3-business-rules.md`, across all 5 categories (ADR-048) | Full itemized list (49 items) at `sot-docs/raw/2-module-specs/Settings/risks-and-open-questions.md` Open Questions §A. Needs direct LBM staff review plus a live production database check — too large and too dependent on tribal knowledge to resolve from code alone. Work through at Settings' own JIT module cycle, not expected to block other modules. |
| Q-30 | SalesOrder has a large batch of unclear/unconfirmed items (~59 across the full open-questions list): unexpanded abbreviations (OSF, CCC, INI, CA, RO Job Hat), duplicate/overlapping field pairs (Order Type vs. Order Type internal, Customer PO Number vs. its alt field, two Contact fields, two Comment fields, Legacy/Generic ID vs. Line Item ID), ~80 not-individually-documented Line Item columns and ~15 Backorder/Buyout columns needing a dedicated follow-up review, several unmapped integration write-targets (OASN Status, document-management integration's real name, the RGN/buyout-PO outbound call's target system), and about 26 live orders with a blank status plus a handful of other unexplained live-data anomalies worth a migration-time audit. | SalesOrder `5-modules/sales-order/4-schema.md` and `3-business-rules.md` | Full itemized list (59 items, SO-OQ-001 through SO-OQ-059) at `sot-docs/raw/2-module-specs/SalesOrder/risks-and-open-questions.md`. Needs direct LBM staff review plus a live production database check — too large to resolve from code alone. Work through at SalesOrder's own JIT module cycle. |
| Q-31 | PurchaseOrder has an older staging table alongside the one used by the standard edit/save/cost pipeline — is the older one still used for anything beyond CSV import? | PurchaseOrder `5-modules/purchase-order/4-schema.md` | Ask LBM staff before finalizing the new staging-table design — determines whether import needs its own separate staging path. |
| Q-32 | Is PurchaseOrder's empty status-picklist table (ADR-142) a dev-database-only gap, or does production never seed it either? | PurchaseOrder `5-modules/purchase-order/4-schema.md` | Needs a production schema comparison or direct LBM confirmation — affects migration but not the ADR-142 decision itself (real enum either way). |
| Q-33 | What does PurchaseOrder's transaction-code value 7 actually mean? Best guess is "waste/excluded from cost" based on a co-located filename, not confirmed. | PurchaseOrder `5-modules/purchase-order/3-business-rules.md` | Ask LBM staff — checked against Products/SearchLineItem's own transaction-code documentation without finding a definitive answer. |
| Q-34 | Can a branch be operationally "closed" today via some manual workaround, or has this genuinely never come up before now (ADR-152 adds a real Active/Inactive field going forward)? | Location `5-modules/location/3-business-rules.md` | Ask LBM staff — helps confirm ADR-152's new field covers the real-world need correctly. |
| Q-35 | Do the TecOrder and Fuse5Connect integrations (credentials exist on the branch header) have any actual consuming code anywhere, or are they genuinely unused? | Location `5-modules/location/integrations.md` | Ask LBM staff — treated as a harder-to-confirm-dead item than this module's other presumed-dead integrations. |
| Q-36 | What does the "SaveLocation"-named Forecasting-module file actually write into Location's own table? | Location `5-modules/location/4-schema.md` | Needs a dedicated code read of the Forecasting module (out of this module's own scope) or LBM staff confirmation. |
| Q-37 | Does Location's own generic delete endpoint block, warn, or silently allow deleting a branch with non-zero on-hand quantity, open orders, or warehouse placements? | Location `5-modules/location/8-api.md` | Needs a dedicated code read or LBM staff confirmation before finalizing the new delete-endpoint's guard behavior. |
| Q-38 | Roughly two dozen Location branch-header and Product-at-Location fields have unclear business meaning (unlabeled columns, unexpanded integration-vendor abbreviations, unconfirmed sibling-column relationships) — what do they mean? | Location `5-modules/location/4-schema.md` | Full list at `sot-docs/raw/2-module-specs/Location/entities-and-fields.md`. Needs direct LBM staff review — too dependent on tribal knowledge to resolve from code alone. |
| Q-39 | AccountStatement has a batch of unconfirmed archive/output questions: what marks an archived statement as display-eligible; the exact trigger for file-path-vs-inline-blob storage precedence; whether a uniqueness constraint exists on (statement-run, account) pairs; whether the legacy browser-add-on PDF path is still functional or superseded; the fax-transmission gateway's actual internals; whether the batch-statement engine has a third undiscovered code path; further UI differences between full/quick statement beyond what's already confirmed; whether the fixed three-month window on one output is intentional; and whether two archive-content-rewriting maintenance scripts are still invoked or dead. | AccountStatement `5-modules/account-statement/4-schema.md` and `outputs.md` | Full itemized list (STMT-OQ-001 through 009) at `sot-docs/raw/2-module-specs/AccountStatement/risks-and-open-questions.md`. Source notes this register is a filtered subset of Accounts', not an independently re-swept one — a dedicated re-verification pass would very plausibly surface more. Needs LBM staff input plus a follow-up code read. |
| Q-40 | ProductTracking has 13 unconfirmed items in one batch: whether a "Product Description" cross-table field registration is a config error or intentional; what "M2" means; why two location-level cost fields' defaults differ from Accounting Net Cost's own GP-basis mapping; whether three specific `change_type` values are live on other tenants; the relationship between two similarly-named Physical Count Report change-type values; whether `change_type = 'Manufacturing'` is live anywhere; which specific writer(s) among ≥26 callers produce the blank Push-To-QuickBooks rows (ADR-168 fixes the field going forward, but the historical writer is still unidentified); whether any of the 22 not-fully-read writer files pass unsafe request-derived values the same way the two confirmed-injectable ones do; and whether PurchaseOrder's receiving-variance PDF recomputes or merely displays this module's cost/QoH figures. | ProductTracking `5-modules/product-tracking/4-schema.md` and `3-business-rules.md` | Full itemized list (13 items) at `sot-docs/raw/2-module-specs/ProductTracking/09-risks-and-open-questions.md` §9.3. Needs LBM staff input plus a full-read sweep of the remaining writer files before finalizing the write contract. |
