# T-001 — Initialize Git repository, connect GitHub remote, base `.gitignore`/README

Status: 5/5 complete

- [x] `git init` at repo root
- [x] Add root `.gitignore` (node_modules, .next, dist, coverage, .env, .env.local, *.log, .DS_Store, `.claude/` — per `6-development/2-folder-structure.md` §17; `.claude/` added after inspecting its contents, local tooling config, not project source)
- [x] Confirm `README.md` reflects current project state (already accurate, no change needed)
- [x] Add GitHub remote (`https://github.com/parimal-c-crest/lbm-erp.git`)
- [x] First commit + push (412 files: `project-docs/`, `CLAUDE.md`, `README.md`, `CHANGELOG.md`, `.gitignore`) — pushed to `main` after developer confirmed go-ahead
