# T-001 — Initialize Git repository, connect GitHub remote, base `.gitignore`/README

Status: 0/5 complete

- [ ] `git init` at repo root
- [ ] Add root `.gitignore` (node_modules, .next, dist, coverage, .env, .env.local, *.log, .DS_Store — per `6-development/2-folder-structure.md` §17)
- [ ] Confirm `README.md` reflects current project state (already close — spot-check, update if stale)
- [ ] Add GitHub remote (`https://github.com/parimal-c-crest/lbm-erp.git`)
- [ ] First commit (existing `project-docs/`, `CLAUDE.md`, `README.md`, `CHANGELOG.md`) — **hold push for explicit go-ahead**, per this session's operating rule on visible/shared actions
