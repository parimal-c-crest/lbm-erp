# T-004 — Scaffold Next.js frontend app

Status: 6/6 complete

- [x] Generated Next.js app in `frontend/` via `create-next-app@latest` (App Router, TypeScript, `src/` dir, Tailwind, ESLint, `@/*` import alias, pnpm)
- [x] `frontend/package.json` name is `frontend`, workspace member confirmed (re-ran root `pnpm install` — all 3 workspace projects linked)
- [x] Added `src/components/{ui,shared}/`, `src/hooks/`, `src/stores/`, `src/lib/`, `src/types/`, `src/config/` skeleton (`.gitkeep` placeholders)
- [x] Added route groups `src/app/(auth)/`, `src/app/(dashboard)/` (empty, `.gitkeep` placeholders — real routes land with App Shell/Chrome, T-013+)
- [x] `pnpm --filter frontend run dev` boots clean ("Ready in 852ms")
- [x] `pnpm --filter frontend run build`/`lint` run cleanly (no errors, no warnings)

Note: generator's Tailwind setup places `globals.css` in `src/app/` (Next.js's own App-Router
convention) rather than a separate `src/styles/` folder as `4-ui/8-frontend-development-standards.md`
§4's illustrative tree showed — not changed here since it's Next's standard pattern; design-token
wiring itself is T-008's job, not this task's.

Removed `frontend/pnpm-workspace.yaml` and `frontend/pnpm-lock.yaml` — `create-next-app` generated
these assuming a standalone project, but they conflict with this repo's actual root-level pnpm
workspace management (T-002). Kept `frontend/AGENTS.md`/`CLAUDE.md` — legitimate Next.js 16 tooling
files, auto-regenerated by `next dev` itself, meant to stay committed.
