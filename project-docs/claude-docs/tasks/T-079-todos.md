# T-079 — Role-resolution endpoint

Status: 2/2 complete

- [x] `GET /uom/groups/{id}/roles/{roleId}/resolve` — `GroupsService.resolveRole`
- [x] BR-021/ADR-192 Base-Type fallback when no explicit Role Assignment exists, response distinguishes `"explicit"` vs `"base_type_fallback"`
